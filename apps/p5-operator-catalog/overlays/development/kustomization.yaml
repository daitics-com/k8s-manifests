apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: daitics-development

resources:
  - ../../base
  - configmap.yaml
  - external-secret.yaml

commonLabels:
  environment: development

patches:
  - path: deployment-patch.yaml

images:
  - name: gitlab-registry.service.daitics.local:30443/daitics-com/p5-operator-catalog
    newTag: "latest"
