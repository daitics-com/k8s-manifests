apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: daitics-production

resources:
  - ../../base

commonLabels:
  environment: production

patches:
  - path: deployment-patch.yaml