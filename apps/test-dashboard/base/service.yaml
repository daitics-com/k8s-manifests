apiVersion: v1
kind: Service
metadata:
  name: test-dashboard
  labels:
    app: test-dashboard
spec:
  type: ClusterIP
  selector:
    app: test-dashboard
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http