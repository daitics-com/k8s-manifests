apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: daitics-production

resources:
  - ../../base
  - configmap.yaml

commonLabels:
  environment: production

patches:
  - path: deployment-patch.yaml

images:
  - name: gitlab-registry.service.daitics.local:30443/daitics-com/dtx-portal
    newTag: "latest"