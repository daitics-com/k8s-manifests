apiVersion: apps/v1
kind: Deployment
metadata:
  name: dtx-portal
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: dtx-portal
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"